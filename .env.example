# Copy to .env (gitignored). Never commit real keys (github-flow rule 2).
# HF token — needed for gated models (e.g. Fanar family access requests).
HF_TOKEN=
# Frontier APIs arrive with the synthesis/eval slices, not setup:
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
